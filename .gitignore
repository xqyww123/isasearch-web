# Generated site output (rebuilt by `python site_source_pages.py publish`
# from the rendered tree + the map artefact); 5+ GB, never versioned.
/published*/

__pycache__/
*.pyc

# Worker local dev secrets (wrangler .dev.vars) — never versioned
worker/.dev.vars
worker/node_modules/
worker/.wrangler/
